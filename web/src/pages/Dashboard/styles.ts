import styled from 'styled-components';

export const Container = styled.div`
  max-width: 1120px;
  margin: 0 auto;
  padding: 4rem 1.6rem;

  overflow-x: hidden;

  p {
    margin: auto;
    color: #e1e1e6;

    font-size: 2rem;
    font-weight: 500;
  }
`;
